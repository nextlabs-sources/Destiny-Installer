# Control Center Uninstallation Instruction

## UI mode

Execute the uninstall.bat/uninstall.sh to trigger the UI uninstaller.

## Silent mode

Execute the uninstall.bat/uninstall.sh with parameter "-s". For example:

    # On Windows
    > uninstall.bat -s
    # on Linux
    $ ./uninstall.sh -s
